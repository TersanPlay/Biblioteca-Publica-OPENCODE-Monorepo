# infra/

Reservado para artefatos de implantação e operação (Docker, CI, compose,
proxy reverso). Hoje o deploy é manual (`pnpm build` + `node`/estático);
nada aqui é exigido pelo build ou pelo runtime.

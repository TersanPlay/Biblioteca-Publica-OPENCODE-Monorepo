### Task 6: Frontend — Add types and API methods

**Status:** DONE

**Files modified:**
- `frontend/src/types/api.ts` — added `BlockedReader` interface after `Reader`
- `frontend/src/features/api.ts` — added `BlockedReader` import, updated `readersApi` with `block` (reason/category params), `unblockBatch`, and `blocked` methods

**Commands run:**
- `npm run build` (in frontend/) — passed
- `git commit` — committed as `643b62b`

**Concerns:** None
